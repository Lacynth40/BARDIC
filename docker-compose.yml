services:
  bardic:
    env_file: .env
    image: bardic-agent
    stdin_open: true 
    tty: true        
    volumes:
      - ./logs:/app/logs
    environment:
      - HF_TOKEN=${HF_TOKEN} # Pulls from your local .env or shell